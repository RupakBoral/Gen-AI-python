# Comprehension:

Short hand notation to write loops and conditions

## List Comprehension: 
- Convert the end result to a list
- Syntax: [item for item in items if condition]

## Set Comprehension:
- Convert the end result to a set
- Syntax: {item for item in items if condition}